{
  "name": "@downite/root",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "bun --filter @downite/web dev:web",
    "dev:server": "cd apps/server && air",
    "test:server": "cd apps/server && go test -cover",
    "db": "cd apps/server && goose -dir ./db/migrations sqlite3 ./tmp/downite.db"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "trustedDependencies": [
    "xycolors"
  ]
}
